name: RDP (Playit Silent)

on:
  workflow_dispatch:

jobs:
  secure-rdp:
    runs-on: windows-latest
    timeout-minutes: 3600

    steps:
      - name: Configure Audio & RDP
        run: |
          Set-Service -Name Audiosrv -StartupType Automatic
          Start-Service -Name Audiosrv
          Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows NT\Terminal Services' -Name "fDisableAudioCapture" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows NT\Terminal Services' -Name "MaxDisconnectionTime" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "SecurityLayer" -Value 0 -Force
          netsh advfirewall firewall delete rule name="Allow-RDP"
          netsh advfirewall firewall add rule name="Allow-RDP" dir=in action=allow protocol=TCP localport=3389

      - name: Create RDP User
        run: |
          $password = "User" + (Get-Random -Minimum 100000 -Maximum 999999)
          $securePass = ConvertTo-SecureString $password -AsPlainText -Force
          New-LocalUser -Name "RDP" -Password $securePass -AccountNeverExpires
          Add-LocalGroupMember -Group "Administrators" -Member "RDP"
          Add-LocalGroupMember -Group "Remote Desktop Users" -Member "RDP"
          echo "RDP_PASS=$password" >> $env:GITHUB_ENV

      - name: Show Login Details
        run: |
          Write-Host "`n==============================================="
          Write-Host "   USERNAME: RDP"
          Write-Host "   PASSWORD: $env:RDP_PASS"
          Write-Host "==============================================="

      - name: Start Playit (Silent Mode)
        run: |
          # 1. Download the LATEST version with the CONFIRMED filename
          $url = "https://github.com/playit-cloud/playit-agent/releases/latest/download/playit-windows-x86_64.exe"
          Invoke-WebRequest -Uri $url -OutFile "playit.exe"
          
          # 2. Start it in background and redirect output to a file (Fixes Freezing)
          $process = Start-Process -FilePath ".\playit.exe" -RedirectStandardOutput "playit.log" -RedirectStandardError "playit_err.log" -PassThru
          
          Write-Host "Playit started in background. Scanning for Claim URL..."
          
          # 3. Read the log file until the Claim URL appears
          $claimUrl = $null
          $counter = 0
          while ($counter -lt 30) {
              if (Test-Path "playit.log") {
                  $content = Get-Content "playit.log"
                  $match = $content | Select-String "https://playit.gg/claim/"
                  if ($match) {
                      $claimUrl = $match.ToString().Trim()
                      break
                  }
              }
              Start-Sleep -Seconds 2
              $counter++
          }
          
          if ($claimUrl) {
              Write-Host "`n========================================================"
              Write-Host " ACTION REQUIRED: Click this link to connect!"
              Write-Host " LINK: $claimUrl"
              Write-Host "========================================================`n"
          } else {
              Write-Error "Could not find Claim URL. Check logs."
              Get-Content "playit.log"
          }
          
          # 4. Keep the runner alive
          while ($true) { Start-Sleep -Seconds 60 }
